version: '3'
services:
  playground:
    container_name: 'python'
    hostname: 'python'
    build: .
    restart: always
    working_dir: '/opt'
    tty: true
    volumes:
      - type: bind
        source: ../opt
        target: /opt
